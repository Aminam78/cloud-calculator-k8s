# ClusterIP service for internal database access.
apiVersion: v1
kind: Service
metadata:
  name: redis-svc # Backend will use this DNS name
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP